#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MODEL_NAME="mlx-community/gemma-4-e4b-it-4bit"

exec bun run "${ROOT_DIR}/../../src/scripts/local-llm-cli.ts" --alias gemma4 --model "${MODEL_NAME}" "$@"
