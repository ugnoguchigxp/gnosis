#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MODEL_NAME="mlx-community/Meta-Llama-3.1-8B-Instruct-4bit"

exec bun run "${ROOT_DIR}/../../src/scripts/local-llm-cli.ts" --alias bonsai --model "${MODEL_NAME}" "$@"
