#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export LOCAL_LLM_MCP_ENABLE_SEMANTIC="${LOCAL_LLM_MCP_ENABLE_SEMANTIC:-1}"
exec bun run "${ROOT_DIR}/src/scripts/local-llm-cli.ts" --alias qwen "$@"
